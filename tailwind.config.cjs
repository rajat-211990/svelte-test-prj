/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js,svelte,ts}"],  // Make sure this is correct
  darkMode: "class",  // Enables dark mode with a CSS class
  theme: {
    extend: {},
  },
  plugins: [],
};
